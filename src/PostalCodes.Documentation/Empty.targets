<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">	
	<Target Name="Build">
		<Message Text="Documentation build needs Sandcastle. Currently only Windows build is supported." />
	</Target>
</Project>
